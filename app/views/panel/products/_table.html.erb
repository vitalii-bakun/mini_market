<table class="table table-striped">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col"><%= t('panel.products.table.title') %></th>
      <th scope="col"><%= t('panel.products.table.price') %></th>
      <th scope="col"><%= t('panel.products.table.available') %></th>
      <th scope="col"><%= t('panel.products.table.description') %></th>
      <th scope="col"><%= t('panel.products.table.control.name') %></th>
    </tr>
  </thead>
  <tbody>
    <% products.each do |product| %>
      <tr>
        <th scope="row"><%= product.id %></th>
        <td><%= link_to product.title, panel_product_path(product) %></td>
        <td><%= number_to_currency_uah(product.price) %></td>
        <td><%= product.available %></td>
        <td><%= product.description %></td>
        <td>
          <div class="container text-center">
            <div class="row">
              <div class="col">
                <% if can? :edit, Product %> <%= link_to t('panel.products.table.control.edit'), edit_panel_product_path(product), class: 'btn btn-primary' %><% end %>
              </div>
              <div class="col">
                <% if can? :destroy, Product %> <%= button_to t('panel.products.table.control.delete'), panel_product_path(product), method: :delete, class: 'btn btn-danger' %> <% end %>
              </div>
            </div>
          </div>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
