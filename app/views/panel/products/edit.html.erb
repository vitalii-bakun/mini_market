<div class="container text-center" style="margin: 10px 0 10px 0">
  <div class="row">
    <div class="col">
      <%= link_to "Show this product", panel_product_path(@product), class:'btn btn-primary' %>
    </div>
    <div class="col">
      <%= link_to "Back to products", panel_products_path, class:'btn btn-primary' %>
    </div>
  </div>
</div>

<h1>Editing product</h1>

<%= render "form", product: @product, url: panel_product_path(@product) %>
